<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/style/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-h-screen flex items-center justify-center bg-gray-900">
        <div class="bg-gray-800 p-8 rounded-lg shadow-xl w-96">
            <h1 class="text-3xl font-bold text-white mb-6 text-center">User Profile</h1>
            
            <div class="space-y-4">
                <div class="bg-gray-700 p-4 rounded-lg">
                    <p class="text-gray-400">Name</p>
                    <p class="text-white text-lg"><%= user.name %></p>
                </div>

                <div class="bg-gray-700 p-4 rounded-lg">
                    <p class="text-gray-400">Email</p>
                    <p class="text-white text-lg"><%= user.email %></p>
                </div>

                <div class="bg-gray-700 p-4 rounded-lg">
                    <p class="text-gray-400">Age</p>
                    <p class="text-white text-lg"><%= user.age %></p>
                </div>

                <div class="bg-gray-700 p-4 rounded-lg">
                    <p class="text-gray-400">Created At</p>
                    <p class="text-white text-lg"><%= new Date(user.createdAt).toLocaleDateString() %></p>
                </div>

                <form action="/user/<%= user.email %>/update" method="GET">
                    <button type="submit" class="w-full bg-green-600 hover:bg-green-700 text-white font-semibold py-3 px-6 rounded-lg text-center transition duration-200 transform hover:scale-105 mb-4">
                        Update Profile
                    </button>
                </form>

                <form action="/" method="GET">
                    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-lg text-center transition duration-200 transform hover:scale-105">
                        Back to Home
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
